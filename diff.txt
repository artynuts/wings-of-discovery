diff --git a/find-artifacts.html b/find-artifacts.html
index bbf06b5..d7cc35b 100644
--- a/find-artifacts.html
+++ b/find-artifacts.html
@@ -92,8 +92,8 @@
 
       .gallery {
         display: grid;
-        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
-        gap: 20px;
+        grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
+        gap: 12px;
         margin-top: 30px;
       }
 
@@ -114,33 +114,39 @@
 
       .image-card img {
         width: 100%;
-        height: 200px;
+        height: 120px;
         object-fit: cover;
         display: block;
       }
 
       .image-info {
-        padding: 15px;
+        padding: 8px;
         background: white;
+        display: flex;
+        flex-wrap: wrap;
+        gap: 4px;
+        align-items: center;
       }
 
       .image-name {
         font-weight: 600;
         color: #333;
-        margin-bottom: 5px;
-        word-break: break-all;
-        font-size: 0.95em;
+        font-size: 0.75em;
+        white-space: nowrap;
+        overflow: hidden;
+        text-overflow: ellipsis;
       }
 
       .image-size {
         color: #999;
-        font-size: 0.85em;
-        margin-bottom: 5px;
+        font-size: 0.7em;
+        white-space: nowrap;
       }
 
       .image-dimensions {
         color: #999;
-        font-size: 0.85em;
+        font-size: 0.7em;
+        white-space: nowrap;
       }
 
       .delete-btn {
@@ -148,10 +154,10 @@
         color: #db3e3e;
         border: none;
         border-radius: 50%;
-        width: 32px;
-        height: 32px;
+        width: 24px;
+        height: 24px;
         cursor: pointer;
-        font-size: 1.1em;
+        font-size: 0.9em;
         display: flex;
         align-items: center;
         justify-content: center;
@@ -174,6 +180,125 @@
         position: relative;
       }
 
+      .spinner {
+        border: 3px solid rgba(138, 62, 219, 0.2);
+        border-top: 3px solid #8a3edb;
+        border-radius: 50%;
+        width: 20px;
+        height: 20px;
+        animation: spin 0.8s linear infinite;
+        flex: 0 0 20px;
+      }
+
+      @keyframes spin {
+        0% {
+          transform: rotate(0deg);
+        }
+        100% {
+          transform: rotate(360deg);
+        }
+      }
+
+      .extraction-overlay {
+        position: absolute;
+        top: 0;
+        left: 0;
+        right: 0;
+        bottom: 0;
+        background: rgba(255, 255, 255, 0.95);
+        display: flex;
+        flex-direction: column;
+        justify-content: center;
+        align-items: center;
+        gap: 12px;
+        border-radius: 15px;
+        z-index: 10;
+      }
+
+      .extraction-overlay p {
+        margin: 0;
+        font-size: 0.85em;
+        color: #333;
+        font-weight: 500;
+      }
+
+      .extraction-spinner {
+        display: flex;
+        align-items: center;
+        gap: 6px;
+        font-size: 0.75em;
+        color: #8a3edb;
+        flex-basis: 100%;
+      }
+
+      .extraction-spinner-icon {
+        width: 12px;
+        height: 12px;
+        border-width: 2px;
+        flex-shrink: 0;
+      }
+
+      .preview-image {
+        border-radius: 10px 10px 0 0;
+      }
+
+      .modal-video-content {
+        display: flex;
+        flex-direction: column;
+        align-items: center;
+        justify-content: center;
+        height: 100vh;
+        color: white;
+        text-align: center;
+      }
+
+      .modal-video-icon {
+        font-size: 4em;
+        margin-bottom: 20px;
+      }
+
+      .modal-video-title {
+        font-size: 1.5em;
+      }
+
+      .modal-video-info {
+        margin-top: 10px;
+        opacity: 0.8;
+      }
+
+      .result-top-match {
+        font-weight: 600;
+        margin-bottom: 10px;
+        color: #333;
+      }
+
+      .file-input-label.drag-over {
+        background: #eef0ff;
+        border-color: #4b1f69;
+      }
+
+      .results-column {
+        display: flex;
+        flex-direction: column;
+        gap: 20px;
+        min-width: 300px;
+        justify-content: center;
+      }
+
+      #analysisSummary {
+        background: white;
+        padding: 15px;
+        border-radius: 10px;
+        border-left: 4px solid #8a3edb;
+      }
+
+      #analysisSummary p {
+        margin: 0;
+        font-size: 0.9em;
+        color: #333;
+        line-height: 1.5;
+      }
+
       .modal {
         display: none;
         position: fixed;
@@ -295,12 +420,17 @@
 
       .loading {
         display: none;
-        text-align: center;
-        padding: 30px;
+        align-items: center;
+        gap: 12px;
       }
 
       .loading.active {
-        display: block;
+        display: flex;
+      }
+
+      .loading p {
+        margin: 0;
+        white-space: nowrap;
       }
 
       .results-grid {
@@ -540,7 +670,7 @@
               <strong>Want to learn more?</strong>
             </p>
             <a
-              href="how-image-detection-works.html"
+              href="ai-image-detection.html"
               style="
                 display: inline-block;
                 background: #8a3edb;
@@ -600,9 +730,6 @@
         </div>
       </div>
 
-      <!-- Selected Images - Full Width Below -->
-      <div class="gallery" id="gallery"></div>
-
       <div class="canvas-buttons">
         <button class="btn-primary analyze-btn" id="analyzeBtn">
           🔍 Analyze
@@ -610,17 +737,20 @@
         <button class="btn-secondary analyze-btn" id="clearBtn">
           🗑️ Clear
         </button>
-      </div>
+        <div class="results-column">
+          <div class="loading" id="loading">
+            <div class="spinner"></div>
+            <p>Analyzing images...</p>
+          </div>
 
-      <div class="loading" id="loading">
-        <div class="spinner"></div>
-        <p>Analyzing images...</p>
+          <div id="analysisSummary" style="display: none">
+            <p id="resultSummary"></p>
+          </div>
+        </div>
       </div>
 
-      <div class="results-header" id="resultsHeader" style="display: none">
-        <h2>Analysis Results</h2>
-        <p id="resultSummary"></p>
-      </div>
+      <!-- Selected Images - Full Width Below -->
+      <div class="gallery" id="gallery"></div>
 
       <div class="results-grid" id="resultsGrid"></div>
     </div>
@@ -681,7 +811,6 @@
       const analyzeBtn = document.getElementById("analyzeBtn");
       const clearBtn = document.getElementById("clearBtn");
       const loading = document.getElementById("loading");
-      const resultsHeader = document.getElementById("resultsHeader");
       const resultsGrid = document.getElementById("resultsGrid");
       const resultSummary = document.getElementById("resultSummary");
 
@@ -758,6 +887,18 @@
 
       function processFiles(files) {
         try {
+          // Clear previous previews and analysis results
+          imageFiles = [];
+          results = [];
+          currentImageIndex = 0;
+          gallery.innerHTML = "";
+          gallery.style.display = "block";
+          stats.style.display = "block";
+          resultSummary.textContent = "";
+          resultsGrid.innerHTML = "";
+          document.getElementById("analysisSummary").style.display = "none";
+          loading.classList.remove("active");
+
           // Filter image and video files
           const filteredFiles = files.filter((file) => {
             const ext = file.name
@@ -778,7 +919,6 @@
           }
 
           // Process files: for videos, extract frames; for images, use directly
-          imageFiles = [];
           let processedCount = 0;
 
           filteredFiles.forEach((file) => {
@@ -794,17 +934,54 @@
               });
               processedCount++;
             } else if (videoExtensions.includes(ext)) {
-              // For videos, create 5 frame placeholders
-              for (let i = 1; i <= 5; i++) {
-                imageFiles.push({
-                  file: file,
-                  type: "video",
-                  isVideo: true,
-                  frameNumber: i,
-                  totalFrames: 5,
-                });
-              }
-              processedCount += 5;
+              // Extract first frame immediately for preview
+              extractVideoFrame(file, 0).then((firstFrameData) => {
+                // Show placeholder with spinner immediately
+                const placeholderIndex = imageFiles.findIndex(
+                  (f) => f.file === file && f.isExtracting
+                );
+                if (placeholderIndex !== -1) {
+                  imageFiles[placeholderIndex].firstFrame =
+                    firstFrameData?.dataUrl;
+                  displayGallery();
+                }
+              });
+
+              imageFiles.push({
+                file: file,
+                type: "video",
+                isVideo: true,
+                isExtracting: true,
+                frameNumber: 0,
+                totalFrames: 0,
+              });
+
+              // For videos, extract all frames immediately
+              extractMultipleVideoFrames(file).then((frames) => {
+                if (frames.length > 0) {
+                  // Remove the placeholder and all its entries for this file
+                  imageFiles = imageFiles.filter((f) => f.file !== file);
+
+                  // Add actual frame entries
+                  frames.forEach((frameData) => {
+                    imageFiles.push({
+                      file: file,
+                      type: "video",
+                      isVideo: true,
+                      frameNumber: frameData.frameNumber,
+                      totalFrames: frames.length,
+                      frameData: frameData.dataUrl,
+                      canvas: frameData.canvas,
+                    });
+                  });
+
+                  // Re-render gallery with actual frames
+                  displayGallery();
+                  updateStats();
+                }
+              });
+
+              processedCount++; // Count the video file itself for now
             }
           });
 
@@ -829,19 +1006,23 @@
           const card = document.createElement("div");
           card.className = "image-card";
           card.addEventListener("click", (event) => {
-            // Don't open modal if delete button was clicked
-            if (event.target.classList.contains("delete-btn")) return;
+            // Don't open modal if delete button was clicked or if extracting
+            if (
+              event.target.classList.contains("delete-btn") ||
+              fileObj.isExtracting
+            )
+              return;
             openModal(index);
           });
 
           const info = document.createElement("div");
           info.className = "image-info";
 
-          const nameEl = document.createElement("div");
+          const nameEl = document.createElement("span");
           nameEl.className = "image-name";
           nameEl.textContent = file.name;
 
-          const sizeEl = document.createElement("div");
+          const sizeEl = document.createElement("span");
           sizeEl.className = "image-size";
           sizeEl.textContent = formatFileSize(file.size);
 
@@ -857,19 +1038,50 @@
           });
           card.appendChild(deleteBtn);
 
+          // Append info first so we can insert image before it
+          card.appendChild(info);
+
           if (fileObj.isVideo) {
-            // Handle video frame placeholders
-            const videoThumbnail = document.createElement("div");
-            videoThumbnail.style.cssText =
-              "width: 100%; height: 180px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); display: flex; align-items: center; justify-content: center; border-radius: 10px 10px 0 0; font-size: 3em;";
-            videoThumbnail.textContent = "🎬";
-
-            const typeEl = document.createElement("div");
-            typeEl.className = "image-dimensions";
-            typeEl.textContent = `Frame ${fileObj.frameNumber}/${fileObj.totalFrames} - Will be extracted on analysis`;
-            info.appendChild(typeEl);
-
-            card.appendChild(videoThumbnail);
+            if (fileObj.isExtracting) {
+              // Display the first frame as preview
+              if (fileObj.firstFrame) {
+                const img = document.createElement("img");
+                img.src = fileObj.firstFrame;
+                img.alt = `${file.name} - Preview`;
+                img.className = "preview-image";
+                card.insertBefore(img, info);
+              }
+
+              // Show spinner in the info section
+              const spinnerContainer = document.createElement("span");
+              spinnerContainer.className = "extraction-spinner";
+
+              const spinnerDiv = document.createElement("div");
+              spinnerDiv.className = "spinner extraction-spinner-icon";
+
+              const text = document.createElement("span");
+              text.className = "extraction-spinner-text";
+              text.textContent = "Extracting images...";
+
+              spinnerContainer.appendChild(spinnerDiv);
+              spinnerContainer.appendChild(text);
+              info.appendChild(spinnerContainer);
+            } else {
+              // Handle video frames - display the extracted frame
+              const typeEl = document.createElement("span");
+              typeEl.className = "image-dimensions";
+              typeEl.textContent = `Frame ${fileObj.frameNumber}/${fileObj.totalFrames}`;
+              info.appendChild(typeEl);
+
+              // Display the extracted frame
+              if (fileObj.frameData) {
+                const img = document.createElement("img");
+                img.src = fileObj.frameData;
+                img.alt = `${file.name} Frame ${fileObj.frameNumber}`;
+                img.className = "preview-image";
+                card.insertBefore(img, info);
+              }
+            }
           } else {
             // Handle image files
             const reader = new FileReader();
@@ -877,7 +1089,7 @@
               const img = document.createElement("img");
               img.src = e.target.result;
               img.alt = file.name;
-              img.style.borderRadius = "10px 10px 0 0";
+              img.className = "preview-image";
 
               const dimensionsEl = document.createElement("div");
               dimensionsEl.className = "image-dimensions";
@@ -886,12 +1098,12 @@
               };
               info.appendChild(dimensionsEl);
 
+              // Insert image before info div
               card.insertBefore(img, info);
             };
             reader.readAsDataURL(file);
           }
 
-          card.appendChild(info);
           gallery.appendChild(card);
         });
       }
@@ -945,14 +1157,14 @@
         if (fileObj.isVideo) {
           // For videos, show a message with frame info
           const modalContent = document.querySelector(".modal-content");
-          modalContent.innerHTML =
-            '<div style="display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; color: white; text-align: center;"><div style="font-size: 4em; margin-bottom: 20px;">🎬</div><div style="font-size: 1.5em;">Video: ' +
-            fileObj.file.name +
-            '</div><div style="margin-top: 10px; opacity: 0.8;">Frame ' +
-            fileObj.frameNumber +
-            " of " +
-            fileObj.totalFrames +
-            '</div><div style="margin-top: 10px; opacity: 0.8;">Frames will be extracted and analyzed when you click Analyze</div></div>';
+          modalContent.innerHTML = `
+            <div class="modal-video-content">
+              <div class="modal-video-icon">🎬</div>
+              <div class="modal-video-title">Video: ${fileObj.file.name}</div>
+              <div class="modal-video-info">Frame ${fileObj.frameNumber} of ${fileObj.totalFrames}</div>
+              <div class="modal-video-info">Frames will be extracted and analyzed when you click Analyze</div>
+            </div>
+          `;
         } else {
           // For images, show the image
           const reader = new FileReader();
@@ -1027,28 +1239,28 @@
         });
       }
 
-      function extractMultipleVideoFrames(file, numFrames = 5) {
+      function extractMultipleVideoFrames(file, intervalMs = 500) {
         return new Promise((resolve) => {
           const video = document.createElement("video");
           video.preload = "metadata";
           const frames = [];
-          let framesCaptured = 0;
 
           video.onloadedmetadata = async () => {
-            const duration = video.duration;
-            const framesToCapture = Math.min(numFrames, Math.ceil(duration));
+            const duration = video.duration * 1000; // Convert to milliseconds
+            let frameNumber = 0;
+            let timestamp = 0;
 
-            for (let i = 0; i < framesToCapture; i++) {
-              const timestamp = (i / (framesToCapture - 1 || 1)) * duration;
-              const frameData = await extractVideoFrame(file, timestamp);
+            while (timestamp <= duration) {
+              const frameData = await extractVideoFrame(file, timestamp / 1000);
               if (frameData) {
+                frameNumber++;
                 frames.push({
-                  frameNumber: i + 1,
-                  timestamp: timestamp,
+                  frameNumber: frameNumber,
+                  timestamp: timestamp / 1000,
                   ...frameData,
                 });
               }
-              framesCaptured++;
+              timestamp += intervalMs;
             }
 
             resolve(frames);
@@ -1079,7 +1291,6 @@
         analyzeBtn.disabled = true;
         loading.classList.add("active");
         resultsGrid.innerHTML = "";
-        resultsHeader.style.display = "none";
 
         try {
           results = [];
@@ -1089,37 +1300,25 @@
               const file = fileObj.file;
 
               if (fileObj.isVideo) {
-                // Extract multiple frames from video
-                const frames = await extractMultipleVideoFrames(file, 5);
-                if (frames.length > 0) {
-                  for (const frame of frames) {
-                    try {
-                      const predictions = await model.predict(frame.canvas);
-                      results.push({
-                        filename: file.name + ` (frame ${frame.frameNumber})`,
-                        dataUrl: frame.dataUrl,
-                        predictions: predictions,
-                        isVideoFrame: true,
-                        timestamp: frame.timestamp,
-                      });
-                    } catch (err) {
-                      console.error("Error analyzing video frame:", err);
-                      results.push({
-                        filename: file.name + ` (frame ${frame.frameNumber})`,
-                        dataUrl: frame.dataUrl,
-                        predictions: [],
-                        error: err.message,
-                        isVideoFrame: true,
-                        timestamp: frame.timestamp,
-                      });
-                    }
-                  }
-                } else {
+                // Use pre-extracted frame data
+                try {
+                  const predictions = await model.predict(fileObj.canvas);
                   results.push({
-                    filename: file.name,
+                    filename: file.name + ` (frame ${fileObj.frameNumber})`,
+                    dataUrl: fileObj.frameData,
+                    predictions: predictions,
+                    isVideoFrame: true,
+                    timestamp: (fileObj.frameNumber - 1) * 0.5,
+                  });
+                } catch (err) {
+                  console.error("Error analyzing video frame:", err);
+                  results.push({
+                    filename: file.name + ` (frame ${fileObj.frameNumber})`,
+                    dataUrl: fileObj.frameData,
                     predictions: [],
-                    error: "Failed to extract frames from video",
+                    error: err.message,
                     isVideoFrame: true,
+                    timestamp: (fileObj.frameNumber - 1) * 0.5,
                   });
                 }
               } else {
@@ -1203,11 +1402,12 @@
 
         // Clear UI
         gallery.innerHTML = "";
+        gallery.style.display = "block";
         stats.style.display = "none";
         document.getElementById("totalImages").textContent = "0";
         document.getElementById("totalSize").textContent = "0 MB";
-        resultsHeader.style.display = "none";
         resultSummary.textContent = "";
+        document.getElementById("analysisSummary").style.display = "none";
         resultsGrid.innerHTML = "";
         loading.classList.remove("active");
         analyzeBtn.disabled = true;
@@ -1220,6 +1420,9 @@
         );
       }
       function displayAnalysisResults(results) {
+        // Hide gallery and show only analysis results
+        gallery.style.display = "none";
+
         resultsGrid.innerHTML = "";
         let highCount = 0;
         let mediumCount = 0;
@@ -1301,7 +1504,7 @@
                 : ""
             }
             <div class="result-filename">🖼️ ${result.filename}</div>
-            <div style="font-weight: 600; margin-bottom: 10px; color: #333;">Top Match: ${topMatchDisplay}</div>
+            <div class="result-top-match">Top Match: ${topMatchDisplay}</div>
             <div class="result-predictions">${predictionsHtml}</div>
           `;
 
@@ -1309,7 +1512,7 @@
         });
 
         resultSummary.textContent = `Analyzed ${results.length} images | High Confidence: ${highCount} | Medium: ${mediumCount} | Low: ${lowCount}`;
-        resultsHeader.style.display = "block";
+        document.getElementById("analysisSummary").style.display = "block";
       }
 
       function formatFileSize(bytes) {
